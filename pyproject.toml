[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "crunchybyt"
version = "0.1.0"
description = "Play old anime episodes on a Tronbyt 64x32 display"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pydantic>=2.6",
    "Pillow>=10.0",
    "yt-dlp>=2024.1",
    "tomli>=2.0; python_version<'3.11'",
]

[project.scripts]
crunchybyt = "crunchybyt.cli:main"
crunchybyt-ingest = "crunchybyt.ingest:main"
crunchybyt-daemon = "crunchybyt.daemon:main"

[tool.setuptools.packages.find]
include = ["crunchybyt*"]
